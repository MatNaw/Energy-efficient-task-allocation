server_efficiency_in_eas		-> Sprawność hosta																[%]
server_energetic_cost			-> Zużycie energii przez hosta									[model CloudSim][W]
link_capacity					-> Pojemność/przepustowość łączy								[const][Jednostka przesyłu]
link_energetic_cost				-> Zużycie energii przez łącze											[const][W]
router_usage_cost				-> Stały koszt eksploatacji (?) routera									[const][W]
demand_tasks					-> Zapotrzebowanie ruchowe pomiędzy maszynami wirtualnymi		[const][Jednostka przesyłu]
demand_tasks_source_nodes		-> Źródłowy host przesyłanego żądania (zapotrzebowania ruchowego)			[const][Number]
demand_tasks_destination_nodes	-> Docelowy host przesyłanego żądania (zapotrzebowania ruchowego) 			[const][Number]
demand_route_length				-> Długość trasy żądania (liczba połączeń) od hosta źródłowego do docelowego[const][Number]
vm_required_efficiency			-> Wymagana (potrzebna?) wydajność hosta dla maszyny wirtualnej					[%]


TODOs:
- (tekst MGR) przykładowe testy poprawności implementacji - uruchomienia na malej sieci dla (jedno pełne uruchomienie na wszystko?):
	a) różnych koszt łączy i routerów
	b) różnych wielkości zadań (demand_tasks) i wymaganych wydajności VM
	c) ?
- "subject to demand_fulfilled:" - check if this condition can be simplified to (route lengths longer than 2?):

# removed for testing -> to be checked with route lengths > 2
    if (demand_in_link_sum[d] != demand_tasks_route_length[d])

NOTE - testy BigDC:
* dla 40 VM (5000 op/s) -> problemy, maksymalnie udało się osiągnąć wyniki dla 7 instancji serwerów
* dla 10 VM (50000 op/s) -> sukces dla każdego typu serwera (20 instancji)